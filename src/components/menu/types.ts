export type TMenuButton = {
    description?: string;
    disabled?: boolean;
    display: string;
    route: string;
};

export type TLoginForm = {
    email: string;
    password: string;
};
